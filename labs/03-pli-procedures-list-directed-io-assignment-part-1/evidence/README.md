# Evidence Map

The supplied DOCX contained **34 embedded screenshots**. They are preserved in document order.

Key evidence visible in the supplied material includes:

- PROC03 source and compile/bind/run JCL.
- Initial PROC03 run with compiler RC=0008 and diagnostics.
- Corrected PROC03 run with no compiler messages and RC=0000.
- PROC03 functional output: `PL/I MAIN PROCEDURE VALIDATED`.
- LISTIO3 source, JCL and `SYSIN DD *` with `10 20`.
- LISTIO3 final output showing values 10 and 20.
- ASSIGN3 source/JCL and final output showing 10, 20 and 30.

Files are numbered in the same order in which they were embedded in the submitted evidence document.
